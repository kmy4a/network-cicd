[project]
name = "network-cicd"
version = "0.1.0"
description = "Network CI/CD with containerlab, ansible and python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible>=13.3.0",
    "napalm>=5.1.0",
    "paramiko>=4.0.0",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
]
